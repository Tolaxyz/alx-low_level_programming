#include <stdio.h>

/**
 * main - functin that prints the value with proper grammar, but the outcome is a piece of art,
 * Description - the function above print the value listed
 * Return: always 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
